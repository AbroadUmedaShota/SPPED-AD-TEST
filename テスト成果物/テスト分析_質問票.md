# テスト分析 質問票

対象: ログイン機能（ユーザー側 `/login` ＋ 管理者側 `/admin/login`）
作成日: 2026-06-03

---

## 1. 質問一覧

| 質問ID | 関連テスト観点ID | 関連テストアプローチID | 質問 | 回答が必要な理由 | 回答者候補 | 優先度 |
| --- | --- | --- | --- | --- | --- | --- |
| Q1 | TV003 | TA002 | 管理者ログイン `/admin/login` のフォームDOMにおけるフィールドのname/id属性および送信後の遷移先URLは何か。また、Playwrightで使用する正規セレクタはどれか。 | E2Eテスト（TA002）の実装に必須。セレクタが未確定だとテストコードが書けない。 | 開発者（inspect実施者） | 高 |
| Q2 | TV011 | TA005 | 一般ユーザーのセッション状態で `/admin/top` へ直接アクセスした場合の正しい期待挙動は何か（HTTPステータス403でブロック / ログインページへリダイレクト / 404 のどれか、またはその他）。 | TV011 の合否判定基準を確定するために必要。Q2 未解消のままでも「管理者画面へ到達できないこと」を最低ラインとして確認するが、リダイレクト先URLやHTTPステータスの厳密な検証ができない。 | 開発者/プロダクト | 高 |
| Q3 | TV007, TV014 | TA003, TA007 | 認証失敗時（誤パスワード/未登録メール）のエラーメッセージ仕様は何か。「登録済みメールか否か」を区別しない汎用メッセージが意図仕様か、区別してもよいか。 | TV014（ユーザー列挙耐性）の合否判定基準を確定するために必要。意図仕様が不明のまま検査すると、区別メッセージが「仕様通り」か「バグ」か判定できない。 | プロダクト | 高 |
| Q5 | TV029 | TA007 | パスワード表示切替UIおよびアカウント記憶チェックボックスが実ステージング（`stg.speed-ad.com/login`）のDOMに存在するか。 | 24番仕様はモック前提のため実stgに同UIが存在するか未確認。存在しない場合はTV029を除外し、SSI003の展開方針を変更する必要がある。 | 開発者（inspect実施者） | 高 |
| Q6 | TV036 | TA005 | ユーザーセッションで管理者側APIエンドポイント（例: `/admin/` 配下のAPIルート）への直接リクエストが拒否されることをE2Eスコープ内で確認することは可能か。また、対象APIエンドポイントが存在するか。 | TV036 の実施可否を判断するために必要。観測要件の制約（実データ更新禁止）に照らして実施方法を確定する必要がある。 | 開発者 | 中 |
| Q7 | TV024, TV025, TV026 | TA005, TA006 | 認証状態と保護ページアクセスに関する期待挙動を確認したい。(a) 認証済みユーザーが `/login` または `/admin/login` へ再アクセスした場合の挙動（ダッシュボードへリダイレクト / ログインフォーム再表示 / その他）。(b) 未認証状態で `/dashboard` へ直接アクセスした場合の挙動（ログインページへリダイレクト / その他）。(c) 未認証状態で `/admin/top` へ直接アクセスした場合の挙動（ログインページへリダイレクト / その他）。 | TV024・TV025・TV026 の合否基準がいずれも未定義のためテスト実施不可。認証ガードの実装方針（未認証時リダイレクト先URL含む）を確認する必要がある。 | 開発者/プロダクト | 中 |
| Q8 | TV023 | TA003 | メールアドレス入力欄の最大文字数制限は仕様として定義されているか。定義がある場合、その上限値は何文字か。 | TV023 の境界値テストを実施するために必要。未定義の場合は境界値テストを省略するかHTML属性（`maxlength`）から実測値を採用する。 | 開発者/プロダクト | 低 |
| Q4 | — | — | パスワード再設定メール送信・新規アカウント作成確定・Googleログインを、実ステージング上で副作用なしに検証できる手段はあるか（専用テストメールアカウント・モックサービスの有無を含む）。 | 現状スコープ外だが、将来的なテスト拡張の判断材料として確認が必要。 | プロダクト/インフラ | 低 |
