# テストケース（人間実行）

対象: ログイン機能（ユーザー側 `/login` ＋ 管理者側 `/admin/login`）
環境: `https://stg.speed-ad.com`
作成日: 2026-06-03
テスト設計: `テスト成果物/テスト設計.md`（TD021・TD026・TD046〜TD052 のうち確定分）

---

## 1. 作成対象

| 対象 | URL |
|---|---|
| ユーザー側ログイン | `https://stg.speed-ad.com/login` |
| 管理者側ログイン | `https://stg.speed-ad.com/admin/login` |

人間（テスター）がブラウザを使って手動で実行するテストケース。認証情報の具体値は含まない。
副作用のある操作（データ送信・削除・確定）は実行しない。

---

## 2. 参照資料

| 資料 | パス |
|---|---|
| テスト設計 | `テスト成果物/テスト設計.md` |
| テスト分析 | `テスト成果物/テスト分析.md` |
| テスト計画書 | `テスト成果物/テスト計画書.md` |
| テスト設計 質問票 | `テスト成果物/テスト設計_質問票.md` |
| 引き継ぎ書 | `テスト成果物/run-test-process_引き継ぎ.md` |

---

## 3. 人間が実行する必要があるテストケース

### 3.1 セキュリティ目視確認（確定分）

| テストケースID | 元テスト設計ID | テスト観点ID | テストアプローチID | 実行区分 | テストレベル/タイプ | 優先度 | テストケース名 | 前提条件 | 入力/データ | 手順 | 期待結果 | 確認方法/証跡 | 関連質問ID | 仕様 | リスクID | 状態 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MAN-001 | TD047 | TV015 | TA007 | 人間実行 | Manual | 高 | パスワード入力欄が通常状態でマスク（type=password）されていること | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること。DevTools を使用できること | なし（フォームへの入力は不要。DOM 属性確認のみ） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. DevTools（F12）を開き Elements タブを表示する。3. `#password` の input 要素を選択し、`type` 属性の値を確認する。4. ページロード直後の状態で確認する（入力前の初期状態）。 | `input#password` の `type` 属性値が `password` であること（マスク状態）。`type` が `text` や `input` 等になっていないこと。 | DevTools Elements タブのスクリーンショット（`type="password"` の属性値が確認できるもの） | なし | R005 / SSI014 | R005 | 作成済み |
| TC-MAN-002 | TD048 | TV016 | TA007 | 人間実行 | Manual | 高 | ログインページおよびログインリクエストが HTTPS で通信されること | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること。DevTools のネットワークタブが使用できること | なし（HTTPS 確認のみ。ログイン実行は任意、フォームの POST リクエストを確認する場合はダミー入力でもよい） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. アドレスバーのプロトコル表示を確認する（鍵アイコンおよび `https://` 表示）。3. DevTools（F12）のネットワークタブを開く。4. ページをリロードし、ログインページ自体のリクエストの `Scheme` 列が `https` であることを確認する。5. 任意: 任意の文字列でログインフォームを送信し、POST リクエストの `Scheme` 列が `https` であることを確認する。 | URLが `https://` で始まること。ブラウザのアドレスバーに鍵アイコンが表示されること。ログインフォームの POST リクエストが `https` スキームで送信されること。 | ブラウザのアドレスバースクリーンショット、DevTools ネットワークタブのスクリーンショット（`Scheme: https` 列が確認できるもの） | なし | R005 / SSI014 | R005 | 作成済み |
| TC-MAN-003 | TD050 | TV030 | TA007 | 人間実行 | Manual | 中 | ログインフォームの送信で CSRF トークンまたは相当の対策が存在すること | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること。DevTools およびページソース表示が使用できること | なし（HTML ソース・HTTP ヘッダ確認のみ） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. 「ページのソースを表示」または DevTools Elements タブでログインフォームの HTML を確認する。3. `<input type="hidden">` の中に CSRF トークン相当のフィールドが存在するかを確認する。4. DevTools ネットワークタブでログインリクエストのリクエストヘッダに `X-CSRF-Token` 等のヘッダが含まれるかを確認する。5. 確認結果を記録する（存在する / 存在しない / 不明 のいずれかを記録）。 | CSRF トークンまたは相当のリクエスト検証機構が存在すること。存在しない場合はその旨を記録する（仕様書に明記なしのため有無の記録のみ）。 | DevTools ネットワークタブのスクリーンショット（リクエストヘッダ）、ページソースのスクリーンショット（CSRF フィールド有無が確認できるもの） | なし | 汎用セキュリティ / R005 | R005 | 作成済み |

### 3.2 セキュリティ確認（XSS）

| テストケースID | 元テスト設計ID | テスト観点ID | テストアプローチID | 実行区分 | テストレベル/タイプ | 優先度 | テストケース名 | 前提条件 | 入力/データ | 手順 | 期待結果 | 確認方法/証跡 | 関連質問ID | 仕様 | リスクID | 状態 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MAN-004 | TD021 | TV038 | TA003 | 人間実行 | Manual | 中 | XSS パターンを入力した場合にブラウザでスクリプトが実行されないこと | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること | `#email` または `#password` フィールドへの入力: `<script>alert(1)</script>`（XSS パターン代表文字列） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. `#email` フィールドに `<script>alert(1)</script>` を入力する。3. `#password` フィールドに任意の文字列（例: `DummyPW_XSS_001`）を入力する。4. ログインボタンをクリックする。5. ブラウザでアラートダイアログが表示されないことを確認する。6. ページに `<script>` タグが埋め込まれたまま表示されないことを確認する。 | ブラウザでアラートダイアログ（`alert(1)` 等）が表示されないこと。ページにスクリプトが埋め込まれたまま実行されないこと（エスケープされているまたは除去されていること）。 | ブラウザのスクリーンショット（アラートなしの状態）、DevTools Console タブのスクリーンショット（スクリプト実行痕跡なし） | なし | 汎用セキュリティ / R001 | R001 | 作成済み |

### 3.3 探索的確認

| テストケースID | 元テスト設計ID | テスト観点ID | テストアプローチID | 実行区分 | テストレベル/タイプ | 優先度 | テストケース名 | 前提条件 | 入力/データ | 手順 | 期待結果 | 確認方法/証跡 | 関連質問ID | 仕様 | リスクID | 状態 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MAN-005 | TD026 | TV039 | TA001 | 人間実行 | Exploratory | 中 | ユーザーセッション確立後の `/dashboard` に別ユーザーのデータが混入していないこと | Lv3アカウント（環境変数 `SPEEDAD_LV3_EMAIL` / `SPEEDAD_LV3_PASSWORD` 参照）でログイン済みであること。`/dashboard` が表示されていること | Lv3アカウントのセッション（ログイン済み状態） | 1. Lv3アカウント（`SPEEDAD_LV3_EMAIL` / `SPEEDAD_LV3_PASSWORD` 参照）でログインする。2. `/dashboard` が表示されたことを確認する。3. 表示されているアカウント情報（氏名・会社名等）が Lv3アカウントに対応するものであることを確認する。4. アンケート一覧に他のユーザーのデータが含まれていないことを目視で確認する。5. ページを複数回リロードし、データが変化しないことを確認する。 | 別ユーザーのデータ（他アカウントの氏名・会社名・アンケート一覧内容）が混入していないこと。Lv3アカウントに対応するデータのみ表示されること。 | ブラウザのスクリーンショット（`/dashboard` の表示内容。個人情報が含まれる場合はマスキング処理を施す） | なし | 要確認 | R002, R004 | 作成済み |

### 3.4 ユーザビリティ確認

| テストケースID | 元テスト設計ID | テスト観点ID | テストアプローチID | 実行区分 | テストレベル/タイプ | 優先度 | テストケース名 | 前提条件 | 入力/データ | 手順 | 期待結果 | 確認方法/証跡 | 関連質問ID | 仕様 | リスクID | 状態 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MAN-006 | TD051 | TV034 | TA007 | 人間実行 | Manual | 高 | キーボードのみで `/login` フォームを操作してログインできること | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること。Lv3アカウント（環境変数 `SPEEDAD_LV3_EMAIL` / `SPEEDAD_LV3_PASSWORD` 参照）が利用できること | Lv3アカウントのメールアドレス（環境変数 `SPEEDAD_LV3_EMAIL` 参照）、Lv3アカウントのパスワード（環境変数 `SPEEDAD_LV3_PASSWORD` 参照） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. マウスを使用せず、キーボードの Tab キーで `#email` フィールドにフォーカスを移動する。3. `SPEEDAD_LV3_EMAIL` の値を入力する。4. Tab キーを押して `#password` フィールドにフォーカスを移動する。5. `SPEEDAD_LV3_PASSWORD` の値を入力する。6. Tab キーを押してログインボタンにフォーカスを移動する（またはそのまま Enter キーを押してフォームを送信する）。7. フォームが送信されることを確認する。 | Tab キーでフォーカスが `#email` → `#password` → ログインボタンの順に移動すること。Enter キーまたは Tab+Enter でフォームが送信されること。ログインが成功して `/dashboard` に到達すること（title「アンケート一覧」）。 | テスト実行記録（フォーカス移動の目視確認）、ブラウザのスクリーンショット（ログイン後の `/dashboard`） | なし | 24番4章 / TA007 | なし | 作成済み |
| TC-MAN-007 | TD052 | TV035 | TA007 | 人間実行 | Manual | 中 | `#email` 入力欄にラベルまたはプレースホルダーが存在し目的（メールアドレス）が識別できること | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること。DevTools が使用できること | なし（DOM 属性確認のみ） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. DevTools（F12）を開き Elements タブで `#email` の input 要素を選択する。3. `<label for="email">` 要素の存在を確認する、または `placeholder` 属性の値を確認する。4. ラベルテキストまたはプレースホルダーテキストが「メールアドレス」「Email」等の目的を示す文字列であることを確認する。 | `#email` 入力欄にラベル要素（`<label>`）またはプレースホルダー（`placeholder` 属性）が存在すること。テキスト内容から「メールアドレス」の入力欄であることが識別できること。 | DevTools Elements タブのスクリーンショット（ラベルまたはプレースホルダー属性が確認できるもの） | なし | 汎用QA | なし | 作成済み |
| TC-MAN-008 | TD052 | TV035 | TA007 | 人間実行 | Manual | 中 | `#password` 入力欄にラベルまたはプレースホルダーが存在し目的（パスワード）が識別できること | Chrome 等のブラウザで `https://stg.speed-ad.com/login` が開ける状態であること。DevTools が使用できること | なし（DOM 属性確認のみ） | 1. ブラウザで `https://stg.speed-ad.com/login` を開く。2. DevTools（F12）を開き Elements タブで `#password` の input 要素を選択する。3. `<label for="password">` 要素の存在を確認する、または `placeholder` 属性の値を確認する。4. ラベルテキストまたはプレースホルダーテキストが「パスワード」「Password」等の目的を示す文字列であることを確認する。 | `#password` 入力欄にラベル要素（`<label>`）またはプレースホルダー（`placeholder` 属性）が存在すること。テキスト内容から「パスワード」の入力欄であることが識別できること。 | DevTools Elements タブのスクリーンショット（ラベルまたはプレースホルダー属性が確認できるもの） | なし | 汎用QA | なし | 作成済み |

---

## 4. 上流成果物への追記・更新

なし（本フェーズでの上流変更なし）。

---

## 5. カバレッジ確認

| テスト設計ID | 対応テストケースID | 実行区分 | 状態 | 出力ファイル | カバー状況 |
|---|---|---|---|---|---|
| TD021 | TC-MAN-004 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（1件 / 期待1件） |
| TD026 | TC-MAN-005 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（1件 / 期待1件） |
| TD047 | TC-MAN-001 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（1件 / 期待1件） |
| TD048 | TC-MAN-002 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（1件 / 期待1件） |
| TD050 | TC-MAN-003 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（1件 / 期待1件） |
| TD051 | TC-MAN-006 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（1件 / 期待1件） |
| TD052 | TC-MAN-007, TC-MAN-008 | 人間実行 | 作成済み | テストケース_人間実行.md | 充足（2件 / 期待2件） |

質問待ちTD（人間実行予定分）→ `テストケース_質問待ち.md` を参照:
TD046（質問待ち: DQ003）, TD049（質問待ち: DQ004）

---

## 6. Case Expansion Ledger（ケース展開台帳）

| テスト設計ID | 期待TC数 | 実TC数 | 不足数 | 対応テストケースID | 分割方針 | 代表抽出理由 | 集約判定ルール | 状態 |
|---|---|---|---|---|---|---|---|---|
| TD021 | 1 | 1 | 0 | TC-MAN-004 | 代表抽出 | XSS パターンは代表1パターンで十分（スモーク範囲） | — | 充足 |
| TD026 | 1 | 1 | 0 | TC-MAN-005 | 代表抽出 | 探索的確認。1セッションで別ユーザーデータ混入なしを観察 | — | 充足 |
| TD047 | 1 | 1 | 0 | TC-MAN-001 | 全件展開 | パスワードマスク確認は1フィールド1確認 | — | 充足 |
| TD048 | 1 | 1 | 0 | TC-MAN-002 | 全件展開 | HTTPS確認は1URL・1リクエストで代表 | — | 充足 |
| TD050 | 1 | 1 | 0 | TC-MAN-003 | 代表抽出 | CSRF確認は1フォームで代表 | — | 充足 |
| TD051 | 1 | 1 | 0 | TC-MAN-006 | 全件展開 | キーボード操作は1フロー（Tab→Enter）で確認 | — | 充足 |
| TD052 | 2 | 2 | 0 | TC-MAN-007, TC-MAN-008 | 全件展開 | `#email`・`#password` の2フィールドを別々に確認 | 各フィールドを個別に Pass/Fail 判定 | 充足 |

**本ファイル合計: 期待TC数 8件 / 実TC数 8件（確定分） / 不足数 0件**

注: TD046（ユーザー列挙耐性・DQ003質問待ち）・TD049（パスワード表示切替・DQ004質問待ち）は `テストケース_質問待ち.md` に分離記録済み。
