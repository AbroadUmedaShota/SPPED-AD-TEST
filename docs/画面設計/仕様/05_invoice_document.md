# 請求書帳票 要件定義書（印刷HTML / PDF）

最終更新日: 2026-04-15

---

## 1. 目的

請求書を以下の2形態で出力できること。

- PDF（詳細画面からダウンロード）
- 印刷（印刷専用ページからブラウザ印刷）

本書は、帳票の「表示項目・並び・計算・改ページ・表示フォーマット」を実装できる粒度で定義する。

---

## 2. 対象・入口

### 2.1 PDF（INV-002 詳細画面）

- 画面: `02_dashboard/invoice-detail.html`
- 操作: `帳票ダウンロード`（`#downloadPdfBtn`）
- 生成対象: `#invoice-sheet-container`

### 2.2 印刷（INV-003 印刷ページ）

- 画面: `02_dashboard/invoice-print.html?id={id}`
- 動作: 画面描画後に `window.print()` を自動実行

---

## 3. データ契約（帳票が期待する `Invoice`）

帳票は `Invoice`（請求書）を1件受け取り、各項目を表示する。金額は `items[]` を正として再計算してもよい。

### 3.1 `Invoice`（必須項目）

| 項目 | 型 | 説明 |
| :--- | :--- | :--- |
| `invoiceId` | string | 請求書番号（表示用に整形済みを推奨） |
| `issueDate` | string | 発行日（`YYYY-MM-DD`） |
| `dueDate` | string | 支払期限（`YYYY-MM-DD`） |
| `billingPeriod` | object | `from/to`（`YYYY-MM-DD`） |
| `corporateName` | string | 宛名（会社） |
| `contactPerson` | string | 宛名（担当者） |
| `plan` | object | `displayName` と `billingType` |
| `addOns` | array | 追加オプション（要約表示用） |
| `status` | string | `unpaid/paid/overdue/canceled` |
| `bankInfo` | object | 振込先情報 |
| `notes` | string/null | 備考（任意） |
| `items` | array | 明細 |

### 3.2 `InvoiceItem`（必須項目）

| 項目 | 型 | 説明 |
| :--- | :--- | :--- |
| `category` | string | `BASE/ADD_ON/ONE_TIME/CREDIT`（金額計算や補助判定に使用） |
| `itemName` | string | 品目1。アンケート依存の行ではアンケート名、アンケート非依存の行では具体課金名を表示する。`従量課金` は表示ラベルに使わない。 |
| `description` | string/null | 品目2。対象月、適用プラン、送信対象件数、無料100件控除、値引き理由などの課金根拠と対象条件を表示する。数量・単価は原則としてここに重複記載しない。 |
| `quantity` | number/null | 数量 |
| `unit` | string/null | 単位 |
| `unitPrice` | number/null | 単価 |
| `amount` | number | 金額 |
| `taxable` | boolean | 課税対象フラグ |

---

## 4. 共通フォーマット要件

### 4.1 日付

- 表示形式: `YYYY年MM月DD日`

### 4.2 金額

- 通貨: JPY
- 表示形式: `¥ 12,345`（3桁区切り）
- 負数は `-¥ 3,000` のようにマイナス表記する。

### 4.3 期間（請求対象期間）

- 表示形式: `YYYY年MM月DD日 ? YYYY年MM月DD日`

---

## 5. 金額計算要件

### 5.1 小計・税・合計

- 課税小計: `sum(item.amount)`（`item.taxable === true` のみ）
- 非課税小計: `sum(item.amount)`（上記以外）
- 消費税: `floor(課税小計 * 0.1)`
- 合計（税込）: `課税小計 + 非課税小計 + 消費税`

---

## 6. 明細出力要件

### 6.1 列

- `No.` / `項目` / `内訳` / `数量` / `単価` / `金額`
- `項目` は品目1、`内訳` は品目2として扱う。

### 6.2 明細粒度

- 帳票上の主要な明細粒度は以下で固定する。
  - `プレミアム利用料`
  - `追加アカウント料金`
  - `名刺データ化費用`
  - `お礼メール送信費用`
  - `クーポン値引き`
- `プレミアム利用料` と `追加アカウント料金` は、同一請求書内で合算される場合でも別行で表示する。アンケート依存の明細では `従量課金` を表示ラベルに使わず、`名刺データ化費用` `お礼メール送信費用` `クーポン値引き` を具体課金名として扱う。
- `クーポン値引き` を表示する場合は、対象課金の値引きとして独立行で表示する。
- 同一アンケート内の表示順は `名刺データ化費用` → `お礼メール送信費用` → `クーポン値引き` を正とする。
- 品目1 / 品目2 の記載方針は以下とする。
  - プレミアム利用料: 品目1は `プレミアム利用料`、品目2は対象月や月額条件
  - 追加アカウント料金: 品目1は `追加アカウント料金`、品目2は `請求対象 Nアカウント` のような請求対象人数のみの表現とする。無料アカウント数は `2` 固定、請求対象は `3アカウント目以降` とするが、これは内部ルールであり請求書表示文言には含めない
  - 名刺データ化費用: 品目1はアンケート名、品目2は `名刺データ化費用 / 適用プラン`
  - お礼メール送信費用: 品目1はアンケート名、品目2は `お礼メール送信費用 / 送信対象 N件 / 無料100件控除`
  - クーポン値引き: 品目1はアンケート名、品目2は `クーポン値引き / 適用理由`
  - 小計行を表示する場合: 品目1は契約系小計名、対象アンケート名、または `請求書小計`、品目2は `小計`
- 現時点では `itemName` / `description` の意味付けで品目1 / 品目2 を表現する。将来的に `itemName1` / `itemName2` などの表示専用フィールドへ分ける可能性はあるが、本仕様では追加フィールドを必須としない。

### 6.3 数量の表示

- `名刺データ化費用.quantity` は名刺データ化件数を表す。
- `お礼メール送信費用` は、データ化された連絡先のうち送信設定画面で選択した件数を母数とする。
- `送信対象件数 <= 名刺データ化費用.quantity` を必須とする。
- `お礼メール送信費用.quantity = max(0, 送信対象件数 - 100)` とする。
- `お礼メール送信費用.quantity = 0` の場合は `お礼メール送信費用` 行を出さない。
- `quantity` が `null` の場合は空欄
- `quantity` と `unit` がある場合は結合して表示（例: `200件`）

### 6.4 並びと小計（印刷ページ）

- 明細の表示順は `プレミアム利用料` → `追加アカウント料金` → 契約系小計 → アンケートごとの明細と小計とする。
- 契約系小計は、`プレミアム利用料` と `追加アカウント料金` の `amount` 合計とする。
- アンケート小計は、同じアンケート名に属する `名刺データ化費用` `お礼メール送信費用` `クーポン値引き` の `amount` 合計とする。
- 契約系小計名は、対象明細に応じて `プレミアムプラン 小計` `グループアカウント料金 小計` `プレミアムプラン・グループアカウント料金 小計` を使い分ける。

### 6.5 集計ID（`AGG-...`）に関する留意点（内部互換）

- 利用者向けの主導線は `INV-...` を正とし、`AGG-...` は legacy/internal 互換用に残す。
- 集計IDの明細には、請求書ごとの「小計」行（`isSubtotal: true`）が混在する。
- 表示画面とPDF（`INV-002`）では、すべての `プレミアム利用料` `追加アカウント料金` を明細先頭にまとめ、その後に契約系小計を 1 回表示する。
- 表示画面とPDF（`INV-002`）では、契約系小計の後に各元請求書ブロックのアンケート明細とアンケート小計を続け、その末尾に `請求書小計` を表示する。
- 印刷ページ（`INV-003`）は現行どおり、各請求書ブロック内で契約系小計・アンケート小計を出した後、その末尾に `請求書小計` を表示する。
- `請求書小計` は元請求書単位の金額確認用であり、契約系小計・アンケート小計の代替ではない。

---

## 7. PDF（INV-002）要件

### 7.1 出力対象

- `#invoice-sheet-container` を A4 縦のPDFとして出力する。

### 7.2 ファイル名

- `invoice-{id}.pdf`（`id` は URL パラメータの値）

### 7.3 改ページ

- 用紙（ページ）単位で改ページされること。
- 明細の改ページは固定件数ではなく、A4帳票内の実表示高さに基づいて動的に行うこと。
- 1ページ目も固定件数は設けず、`項目` `内訳` の折り返し量を含む実表示高さに応じて収まる範囲まで表示すること。
- 2ページ目以降も固定件数を保証せず、明細表とページ番号の間に収まる範囲で出力すること。
- 実明細が少ないページでは、残余高さに収まる未記載行を追加し、帳票下端まで罫線を維持すること。
- 複数ページの場合、ページ番号が `{current}/{total}` で表示されること。

---

## 8. 印刷（INV-003）要件

### 8.1 印刷起動

- 初期描画後に印刷ダイアログを自動で起動する。

### 8.2 備考

- `notes` が空の場合は備考ブロック自体を表示しない。

---

## 9. データ品質（帳票の整合性を保つための必須条件）

帳票は `items[]` をもとに計算・表示を行うため、データ側で以下を必須とする。

- 全明細に `category` を必ず付与する（未設定明細は印刷ページで出力されない）
- 全明細に `taxable` を必ず付与する（税計算のブレを防ぐ）
- `amount` が未設定の場合は `unitPrice * quantity` が成立すること（両方 `number`）

---

## 10. 手動テスト

`docs/ハンドブック/テスト/invoice_manual_checklist.md` を参照。
