# システム構成

> [!CAUTION]
> このページは Backlog wiki の旧資料バックアップです。プロトタイプ期の資料であり、現行運用の正本ではありません。機微情報を含む可能性があります。

## Metadata

- wikiId: `1085879`
- projectId: `153515`
- source: `SPDAD/Home`
- updated: `2025-12-12T04:19:43Z`

## Backlog 原文

# システム構成図
<CloudFront 採用後版>
![image][tmp.png]
<CloudFront 採用前版>
![image][スクリーンショット 2025-04-24 212245.png]

## Mermaid 形式

```
flowchart TB

    %% --- クライアント ---
    subgraph Client["クライアント / ブラウザ"]
    end

    %% --- CloudFront / S3 ---
    Client -->|"署名付きURLで画像取得"| CloudFront

    subgraph CDN["CloudFront"]
        CF["署名付きURLアクセス制御<br/>OAC"]
    end

    CloudFront -->|"OAC 経由のみ許可"| S3

    subgraph S3["S3（speed-ad-files）"]
        S3Folders["/[環境名]/cards"]
    end

    %% --- アプリケーション ---
    Client -->|HTTPリクエスト| WebServer

    subgraph Lightsail["AWS Lightsail"]
        WebServer["Webサーバ<br/>Laravel"]
        DBServer["DBサーバ<br/>MariaDB"]
    end

    WebServer -->|"データ読み書き"| DBServer

    WebServer -->|"画像分析リクエスト"| VisionAPI
    WebServer -->|"メール送信"| MailServer


    %% --- Webサーバ → S3 アップロード ---
    WebServer -->|"画像アップロード"| S3

    %% --- Webサーバ → CloudFront 署名付きURLを発行 ---
    WebServer -->|"署名付きURL生成"| CloudFront


    %% --- メールサーバ・Vision API ---
    subgraph MailServer["メールサーバ<br/>SMTP / XServer"]
    end

    subgraph VisionAPI["Google Vision API"]
    end
```



# DNSについて
(未確定情報)
https://www.chatwork.com/#!rid310404873-2015719645994295296

## 添付情報

- 未取得 attachment: `スクリーンショット 2025-04-24 212245.png` (attachmentId=`607473`, size=`40621`)
- 未取得 attachment: `tmp.png` (attachmentId=`660697`, size=`44211`)

## Shared Files

- sharedFiles なし
