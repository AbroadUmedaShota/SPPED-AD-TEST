## 対象 Issue
Closes #287

## 変更内容
- `02_dashboard/surveyCreation-v2.html` を要件定義書に沿って完全リファクタリング（スクラッチから再構築）。
- 古いUI仕様（設問グループ化など）の残骸を削除。
- 3エリア構成（設定カラム・設問カラム・アウトラインパネル）を Tailwind CSS の Grid System で実装。
- スマホ・タブレット（md/sm）サイズ時のアクションバー固定表示（`fixed bottom-0`）とレスポンシブな2カラムフォールバックに対応。
- 多言語化設定、およびサンクス画面・名刺データ・お礼メール設定のインラインUI統合を実現。

## テスト内容（手動確認項目）
- 自動テストが存在しない静的モック画面のため、要件定義書記載のID・クラスが既存JS（`surveyCreation-v2.js`）から正しく参照できるDOM構造となっているかを静的に検証しました。
- ブレークポイント（lg: 3カラム, md/sm: 2カラムまたは1カラム + bottom固定アクションバー）が正しく制御されるようクラス定義を確認しました。
