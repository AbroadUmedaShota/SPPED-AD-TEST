# 不具合・指摘一覧（run3）

作成日: 2026-06-03
証跡フォルダ: `テスト成果物_全画面/report/20260603131634_クロール/`

## 重大度定義

| 重大度 | 基準 |
| --- | --- |
| 高 | 本番リリース前に必須対応。法務要件・業務フロー阻害・セキュリティ影響あり |
| 中 | 正式リリース前の対応を推奨。機能誤動作または設計との乖離 |
| 低 | 動作に致命的影響なし。設計確認・改善推奨レベル |

---

## run3 新規起票（DEF-301〜DEF-304）

### DEF-301: `/admin/invoice` でリソース500エラー

| 項目 | 内容 |
| --- | --- |
| ID | DEF-301 |
| 重大度 | 高 |
| 画面 | A-06（請求管理） / `/admin/invoice` |
| 再現手順 | 管理者ログイン後、`/admin/invoice` へアクセスする |
| 期待値 | HTTP 200 でページが表示され、コンソールエラーが0件であること |
| 実測値 | HTTP 200（ページは表示される）、コンソールエラー1件（リソース読み込みの500エラー） |
| 状態 | 未対応 |
| 証跡 | `admin_crawl.json` No.7: `consoleErrors: 1` / `admin_crawl.md` 行7: `console err = 1` |
| 備考 | HTTP 200 だがページ内リソース取得に500エラーが発生。請求書データの取得APIや静的リソースに問題がある可能性がある（要調査）。請求書管理は業務上重要な画面であるため高重大度とした |

---

### DEF-302: support サイトの法務・公開ページが 403

| 項目 | 内容 |
| --- | --- |
| ID | DEF-302 |
| 重大度 | 高 |
| 画面 | U-19（法務ページ）/ P-01（導入事例 一覧） / 対象ルート: `https://stg.support.speed-ad.com/terms/` / `/privacy/` / `/tokushoho/` / `/customer-voices/` |
| 再現手順 | `https://stg.support.speed-ad.com/terms/`（または `/privacy/` / `/tokushoho/` / `/customer-voices/`）へアクセスする |
| 期待値 | HTTP 200 でページが表示されること（仕様: 法定表示は常時到達可能にする、導入事例は信頼補強コンテンツ） |
| 実測値 | HTTP 403、タイトル空白、コンソールエラー1件（Failed to load resource: 403） |
| 状態 | 未対応 |
| 証跡 | `smoke_rest.json`: `/terms/` `status:403` / `/privacy/` `status:403` / `/tokushoho/` `status:403` / `/customer-voices/` `status:403` / `smoke_rest.md` 対応行 |
| 備考 | 利用規約・プライバシーポリシー・特定商取引法表示は法定表示であり、常時到達可能であることが法務要件となる。stg環境でのアクセス制限（IPまたは認証）の可能性あり、本番環境での確認が必要。本番でも同様の場合は高緊急対応が必要 |

---

### DEF-303: `/survey-thanks` への GET 直アクセスで 405

| 項目 | 内容 |
| --- | --- |
| ID | DEF-303 |
| 重大度 | 低〜中（要設計確認） |
| 画面 | U-04（アンケート回答完了） / `/survey-thanks` |
| 再現手順 | ブラウザのアドレスバーから `https://stg.speed-ad.com/survey-thanks` へ直接 GET アクセスする |
| 期待値 | 設計方針による（a: POST後のみ表示でよい場合は405で仕様通り、b: 直アクセスをリダイレクト等で丁寧にハンドリングする設計の場合は要対応） |
| 実測値 | HTTP 405、エラーメッセージ「The GET method is not supported for route survey-thanks. Supported methods: POST. (405 Method Not Allowed)」、コンソールエラー1件 |
| 状態 | 設計確認待ち |
| 証跡 | `smoke_rest.json`: `/survey-thanks` `status:405` / `smoke_rest.md` 対応行 |
| 備考 | POST専用エンドポイントとして設計されている場合は仕様通りの挙動。ただしユーザーがURLを直接開いた場合やブックマーク等でアクセスした場合の体験（エラー画面が表示される）についてPM・デザインの方針確認を推奨 |

---

### DEF-304: `/group-edit` が `/dashboard` へリダイレクトされ、タイトルが「アンケート一覧」

| 項目 | 内容 |
| --- | --- |
| ID | DEF-304 |
| 重大度 | 中 |
| 画面 | U-10（グループ管理） / `/group-edit` |
| 再現手順 | 認証済みユーザーで `/group-edit` へアクセスする |
| 期待値 | `/group-edit` がグループ管理画面（U-10）を表示し、タイトルが「グループ管理」（または相当する画面名）であること |
| 実測値 | `finalUrl: /dashboard`（リダイレクト発生）、タイトル「SpeedAd - アンケート一覧」、コンソールエラー0件 |
| 状態 | 要確認（リダイレクト仕様か不具合かの判断が必要） |
| 証跡 | `user_crawl.json` No.3: `url: /group-edit`, `finalUrl: /dashboard`, `title: SpeedAd - アンケート一覧` / `smoke_rest.json`: `/group-edit` `title: SpeedAd - アンケート一覧`（smoke_rest.jsonはfinalURL情報なし） |
| 備考 | ダッシュボードナビゲーション（dashboard_navlinks.json）には「グループ管理::https://stg.speed-ad.com/group-edit」として導線が存在するにもかかわらず、アクセスするとダッシュボードにリダイレクトされている。グループ機能が未リリース・プラン制限・フィーチャーフラグ等によるリダイレクトなのか、ルーティング設定の誤りなのかを確認する必要がある |

---

## 他 run からの継続参照

以下は run2 以前に起票された不具合で、run3 においても未解消のため参照記載する。詳細は各 run の不具合一覧を参照すること。

| ID | 重大度 | 概要 | 元 run | 状態 |
| --- | --- | --- | --- | --- |
| REG-001 | 中（要確認） | 名刺データ化の回答入力画面で Enter キー押下時に 405 エラーが発生する | run2 | 未対応（run3 未再確認） |
| SEC-R2-1 | 高 | stg 環境のデバッグモードが ON になっている | run2 | 未対応（run3 未再確認） |
| OBS-003 | 中（要確認） | 権限レベル Lv1 のユーザーが実績画面（`/admin/achievements`）に到達できる | run2 | 未対応（run3 未再確認） |

> 注意: REG-001・SEC-R2-1・OBS-003 の run3 での再確認は実施していない。run3 のクロール結果では SEC-R2-1 についての新たな証跡取得なし。OBS-003 については `/admin/achievements` が HTTP 200 で到達できることは run3 で確認済み（admin_crawl.json No.11）。権限チェックの詳細は機能テストフェーズで確認が必要。
