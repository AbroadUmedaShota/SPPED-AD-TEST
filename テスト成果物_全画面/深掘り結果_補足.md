# 深掘り再プローブ 結果補足（run3）

実施 2026-06-03 / read only / 証跡: report/20260603140210_利用者深掘り, report/20260603140437_管理者モーダル

## 1. 利用者 要再確認 → 実機再検証で確定（大半が正常動作）

| 項目 | 自動再検証 | 判定 |
|---|---|---|
| U-01 アンケート新規作成 | **モーダルが開く** | ✅ 正常 |
| U-01 データダウンロード | **DLオプションモーダルが開く** | ✅ 正常 |
| U-01 絞り込み select | アカウント/会社別フィルタ（個人/株式会社SpeedAd/TEST/【Abroad】テストグループ_梅田）が機能 | ✅ 正常（status絞り込みでなくアカウント絞り込み） |
| M-05 QR モーダル | **URL表示=有 / コピーボタン=有 / QR画像=有** | ✅ 正常 |
| M-01 アカウント情報モーダル | **モーダルが開く** | ✅ 正常 |
| U-15 パスワード変更 | パスワード欄3つ・**不一致時にエラー表示** | ✅ 正常（バリデーション動作） |

→ 機能テストで WARN/SKIP だった利用者側の主要項目は、**実機で正常動作を確認**。実不具合ではなかった。

## 2. 未マッピング/未検証として残る項目

| 画面 | 状況 | 次アクション |
|---|---|---|
| U-17 プレミアム関連 | 当アカウント(crew02)から導線・`/premium` `/premium/signup` `/plan` いずれも未検出（既にプレミアム or 別ルートの可能性） | 要確認: 正しいルート/非プレミアムアカウントで再検証 |
| U-11→U-12/13 請求書詳細・印刷 | 当アカウントの請求書が **0件** のため詳細/印刷に到達できず | 請求書があるアカウント/状態で再検証 |
| U-03/04 回答・回答完了 | run2 で実投入済み（/questionnaire_answer・/survey-thanks）。直GET 405 は DEF-303 で起票 | 済 |

## 3. 管理者モーダル → 実DOM調査で正常動作を確定（当初「反応なし」はプローブ限界）

実DOM調査（report/20260603141141_管理モーダル調査）で、管理者モーダルの実クラスを特定し再検証した結果、**正常に開くことを確認**:

| モーダル | 実セレクタ | 結果 |
|---|---|---|
| クーポン新規作成 | `#new-coupon`（class `modal-content` / 親 `modal-open new-coupon`） | ✅ **モーダル表示・入力欄7（クーポン名/コード/作成日/使用可能期限/割引率/使用可能回数/メモ）＋「作成」ボタン**。証跡 `クーポン管理_modal_新規作成.png` |
| オペレーター新規作成 | `#modal`（Tailwind `relative top-20 ...` / `#modal-title`） | ✅ **モーダル表示・可視入力7** |

- 当初の func-admin-modals での「反応なし」は、**汎用セレクタ `.modal:visible`（完全一致）が実クラス `modal-content` / `#modal` に一致しなかったプローブ限界**であり、**実不具合ではない**。
- 作成/保存/送信/削除の確定は副作用回避のため未実行（モーダル表示・フォーム存在までを確認）。
- 残り（クーポン編集/一括/利用履歴・オペレーター編集・利用者メール送信モーダルの個別フォーム検証）は同手法で順次確定可能。

## 4. 確定している事実（再掲）

- 機能テスト範囲（管理10画面・利用者6画面群）で **FAIL 0・コンソールエラー 0**。
- 利用者側の要再確認は実機で正常動作を確認。
- 残る未確定は「管理者モーダルの自動開閉」「プレミアム導線」「請求書詳細（データ依存）」「業務シナリオ通し」「レスポンシブ/a11y/性能」。
