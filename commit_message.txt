fix: Resolve module import/export errors in JS

- Corrected module import/export inconsistencies between `main.js`, `invoiceDetail.js`, and `utils.js`.
- Moved loading and message utility functions (`showLoading`, `hideLoading`, `showMessage`) from `invoiceDetail.js` to `utils.js` to make them reusable.
- Modified `invoiceDetail.js` to import dependencies directly from `utils.js` and other specific modules, removing the dependency on `main.js`.
- Removed the unnecessary initialization call for `invoiceDetail.js` from `main.js`.