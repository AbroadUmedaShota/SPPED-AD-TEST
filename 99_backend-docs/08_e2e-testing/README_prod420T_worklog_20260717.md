# 本番E2E作業ログ — PRD-SCN-420T 多言語 名刺言語判定ロジック真理値表逆算＋振分・入力完遂

- 日付: 2026-07-17（実行 10:24〜11:13 JST）
- 環境: 本番 https://speed-ad.com/
- 対象アンケート: 424「多言語ルーティングテスト＠簡体字繁体字」（ja/繁/簡）／425「＠英語簡体字」（ja/en/簡）／426「＠英語繁体字」（ja/繁/en）。いずれも会期 2026-07-17〜2026-07-31・事前回答0件
- 手段: Playwright（Python）実画面操作のみ（実クリック・実ファイル選択・実送信。DOM直叩きなし）
- 送信: 計32件（回答ID 29147〜29178・全て通常回答計上）。名刺は423作成の8枚（ja/en/簡/繁×2・実PIIなし・簡繁字形差入り）を流用
- テストプラン: 多言語判定ロジック真理値表テストプラン_20260716.md（2026-07-17に「対応可能言語の1言語=1アカウント分離」「入力完遂までスコープ拡大」を反映）

---

## 1. 結論（真理値表・確定）

**「名刺の入力言語」の判定は2段構え。①回答言語=日本語なら名刺が何語でも「日本語」。②回答言語≠日本語なら名刺OCRの文字種のみで決まり、回答言語の値（en/簡/繁の別）は判定に一切影響しない。**

| 回答\名刺 | ja名刺 | en名刺 | 簡名刺 | 繁名刺 |
|---|---|---|---|---|
| **ja**（426・多言語ON） | 日本語 | **日本語** | **日本語** | **日本語** |
| **en**（425） | 日本語 | 判定不能 | 判定不能 | **日本語** |
| **簡**（424） | 日本語 | 判定不能 | 判定不能 | **日本語** |
| **繁**（424） | 日本語 | 判定不能 | 判定不能 | **日本語** |

各セル2枚送信（計32件）で全セル2/2再現。太字が事前仮説（7/15「一致→その言語／日本語絡み不一致→日本語／日本語なし不一致→判定不能」）と食い違った点。

### ⚠️ 適用範囲の限定（2026-07-17 追記・重要）

**本真理値表は「自由記述設問なし・名刺添付のみ」のアンケート（424-426の構成）に対する挙動である。** 420（7/10実走・自由記述設問Q.01_テストあり・本文を選択言語で記述して送信）では **英語判定2件・中国語判定2件が実際に出ていた**（README_prod420_worklog_20260715.md: 未入力13件の内訳=日本語4/英語2/中国語2/判定不能5、「一致→その言語」が全29レコードと整合）。420と424-426の条件差は本文テキストの有無のみ（ベース言語=日本語固定・対応言語構成は同等）。

→ **修正仮説: 判定エンジンは「回答本文のテキスト言語」を主要な入力として読む。** 本文があれば「本文言語×名刺言語」で一致→その言語が機能する（420）。本文がない場合は名刺OCRのみとなり、漢字系→日本語／それ以外→判定不能に縮退する（本テスト）。回答言語プルダウンの値そのものは判定に効かない（ja選択時の全件日本語は「本文なし×ja」の縮退動作の可能性を含め要追試）。**決着には「多言語ON＋自由記述設問あり」のアンケートで en×en本文英語 等の追試が必要**（未実施・下記§6-1）。

### 最重要の発見

1. **「英語」「中国語（簡体字）」「中国語（繁体字）」という判定値は32送信で一度も出現しなかった**（※上記の適用範囲限定つき＝本文なしアンケートでの挙動）。en×en・簡×簡・繁×繁の完全一致ケースですら判定不能または日本語になる。→ 名刺のみ構成のアンケートでは言語別オペレーターチーム（チームB=orange/チームC=西安の中国語チーム）への言語ルーティングが発生しない。仕様意図か実装不具合か、開発会社への最重要確認事項。
2. **繁体字名刺は常に「日本語」判定**（OCRが漢字を日本語と解釈している可能性）。簡体字名刺は「判定不能」になるので簡繁で挙動が非対称。
3. **判定不能→全オペレーターに配信**（フォールバック実測）。滞留せず、納品漏れは発生しない。420Mで恐れた「判定不能→誰にも出ない」は否定された。
4. 423（多言語OFF）のja行と426（多言語ON）のja行は同一結果。「多言語OFFだから機械的にja付与」ではなく、**回答言語=jaなら判定が常に日本語**（ONでも同じ）。423の仮説A/B（判定ルールか機械付与か）は判別不要になった＝どちらのレジームでも出力同一。

## 2. 送信と判定・振分の対応（32件全件）

回答IDは送信順の連番で全件対応（判定値はCSV実DL、振分は送信ごとに4アカウント即時観測）。

| ブロック | 回答ID | アンケート | 回答言語 | 名刺 | 判定値 | キュー+1が出たアカウント |
|---|---|---|---|---|---|---|
| B1 | 29147/29148 | 425 | 英語 | ja | 日本語 | 02のみ |
| B1 | 29149/29150 | 425 | 英語 | en | 判定不能 | **全員** |
| B1 | 29151/29152 | 425 | 英語 | 簡 | 判定不能 | **全員** |
| B1 | 29153/29154 | 425 | 英語 | 繁 | 日本語 | 02のみ |
| B2 | 29155/29156 | 424 | 簡体字 | ja | 日本語 | 02のみ |
| B2 | 29157/29158 | 424 | 簡体字 | en | 判定不能 | **全員** |
| B2 | 29159/29160 | 424 | 簡体字 | 簡 | 判定不能 | **全員** |
| B2 | 29161/29162 | 424 | 簡体字 | 繁 | 日本語 | 02のみ |
| B3 | 29163/29164 | 424 | 繁体字 | ja | 日本語 | 02のみ |
| B3 | 29165/29166 | 424 | 繁体字 | en | 判定不能 | **全員** |
| B3 | 29167/29168 | 424 | 繁体字 | 簡 | 判定不能 | **全員** |
| B3 | 29169/29170 | 424 | 繁体字 | 繁 | 日本語 | 02のみ |
| B4 | 29171〜29178 | 426 | 日本語 | ja/en/簡/繁×各2 | 全て日本語 | 02のみ（8/8） |

- オペレーター対応可能言語（検証中固定・適用10:26〜復元11:12）: **02=日本語のみ／03=繁体字のみ／04=英語のみ／05=簡体字のみ**（1言語=1アカウント分離）
- 「アンケートの回答言語」列は選択言語がすべて正しく記録された（英語/中国語（簡体字）/中国語（繁体字）/日本語）＝回答言語の記録精度OK
- ベースライン: 送信前に4アカウント全部 G1〜G8=0件（グローバルキューに他アンケートの滞留なし・帰属一意）

## 3. 振分（ルーティング）の実測まとめ

- **日本語判定 → 日本語対応アカウントのみ**（02に20件。繁のみ03・英のみ04・簡のみ05には一切出ない）
- **判定不能 → 全アカウント配信**（言語設定に関係なく02/03/04/05全員の作業可能件数が+1）
- 英語・簡体字・繁体字判定のレコードは発生しないため、当該言語のみ設定のオペレーターに「専属の」作業は一切来ない（今回の04/05/03は判定不能の共有分しか見えなかった）
- グループ前進後の**振り直しなし**（日本語判定はG1→G6を通して02のみ、判定不能は全員のまま）

## 4. 入力完遂（2026-07-17ユーザー判断のスコープ）

32レコード全件をデータ化入力でG1〜G8まで完全ドレイン（最終状態: 4アカウント全部 全グループ0件）。

- 実施順: 04（英のみ・判定不能を1巡目）→ 05（簡のみ・2巡目）→ 02（日のみ・日本語20件+残り）→ 03（繁のみ・残り2件系）
- **同一性確認**: 入力画面の名刺画像ファイル名に回答IDが埋まる（`00222004xx_000291xx_1.jpg`）ため全作業アイテムをIDで照合。未知ID出現ゼロ＝実在レコードには一切触れていない
- 2値一致の実態: 項目ごとに「OCR＋人間1致」または「人間2人一致」で確定。1オペレーターが入力するとそのアカウントのキューからは消えるが、確定未了の項目は他オペレーターのキューに残る（04入力後も03/05に残件が見えた）
- 非対応言語オペレーターでも判定不能レコードの入力は可能（04=英のみが判定不能を通しで入力できた）

## 4.5 再現ループ（2026-07-17 12:08〜12:14・ユーザー指示の追加確認）

en/簡/繁の3行×4名刺を各1枚再送信（計12件・回答ID 29179〜29190）し、**12/12が§1の真理値表と完全一致**（各セル通算3/3再現）。判定不能セルから判定不能以外の値は出ず、「英語」「中国語」判定は今回も出現ゼロ。

- 実行時の設定は**復元後の実運用構成**（02/03=日+繁／04/05=英+簡）: ja・繁名刺（日本語判定）→02/03のみ+1、en・簡名刺（判定不能）→4アカウント全員+1 — 分離構成のときと同じ振分ルールを実運用構成でも再確認
- 副次観測: 設定復元（03が日本語対応に復帰）した瞬間、02が1人目入力済みで「人間2人目」待ちだった項目群が03のキューに出現した（対応可能言語の変更が既存キューの可視性に即時反映される実証）
- 12件も入力完遂済み（03→02→04→05＋03の2周目でG1〜G8全ドレイン・最終確認12:30全アカウント0件）

## 5. DL言語フィルタの実測（回答データCSV・424）

- フィルタ選択肢は**実回答から動的生成**: 繁体字回答が入ると「中国語（繁体字）」が出現（425は英語回答のみのため英語/日本語だけ）。多言語OFF（423）ではフィルタ自体が出ない（7/16確認と整合）
- フィルタの一致条件は**「アンケートの回答言語」OR「名刺の入力言語」**: 「日本語」指定→判定値=日本語の8行（回答言語は簡/繁）／「簡体字」指定→回答言語=簡の8行（判定不能含む）
- **判定不能レコードの取りこぼしなし**（回答言語側のフィルタで必ず拾える）。ただし同一行が複数フィルタに重複して出るため、フィルタ別DLを合算すると重複が生じる点は突合時の注意

## 6. 開発会社への確認事項（起票・Wiki追記の材料）

0. **【追試が先】判定エンジンの入力に「回答本文テキスト」が含まれるかの確定**（§1適用範囲限定を参照）。420（本文あり）では英語・中国語判定が出た／424-426（名刺のみ）では出ない。多言語ON＋自由記述設問ありのアンケートで en回答×英語本文×英語名刺 等を送る追試で決着する。追試前に本件を「判定が出ない不具合」として起票しないこと（本文なし構成に限る事象の可能性が高い）
1. **名刺のみ（自由記述設問なし）のアンケートで名刺言語判定が「英語」「中国語（簡/繁）」を出力しない**のは仕様か不具合か（一致ケースでも判定不能/日本語）。Wiki 25/61/63に判定ルールの正本記載なし
2. 繁体字名刺が「日本語」判定になる（簡体字は判定不能）非対称の意図
3. 回答言語=日本語のとき名刺言語を見ずに「日本語」確定する仕様の意図（外国語名刺が日本語オペに集約される＝データ化品質の論点、423の結論と同じ）
4. 判定不能→全員配信のフォールバック仕様の明文化（滞留はしない＝納品漏れなしを実測確認済み）
5. DLフィルタのOR一致（回答言語OR判定値）と重複出力の仕様明文化

## 7. クリーンアップ・復元

- **オペレーター対応可能言語は復元済み**（11:12・保存後再オープンで検証OK）: 02=日+繁／03=日+繁／04=英+簡／05=英+簡（変更前と同一。language_idsのみ変更、所属・権限・ステータスは終始非変更）
- テスト回答32件（29147〜29178・通常回答計上）は424/425/426に残存。テスト専用アンケートのため残置可（プラン既定）。データ化入力は完遂済みのため入力キューへの残留なし
- 送信時のtmp画像: 今回は全送信が完了まで到達しており孤児は発生していない見込み
- DLしたCSV・証跡はセッションscratchpad `420t/`（リポ未コミット・認証情報なし）

## 8. 証跡

- scratchpad `420t/`: evidence/（設定前後・送信各ステップ・キュー観測 計200枚超）・downloads/（b1_mid/b1_final=425、b2_final/b3_final=424、b4_final=426、flt_*=フィルタ別424）・settings_log.md（設定変更・復元の記録）・scripts/（送信/キュー観測/入力/DL/設定の各スクリプト）
- 判定値の人間確認（BUG-003）: CSV実物は downloads/ に保存済み。**ユーザー目視確認をお願いします**（§2の表と突合）

## 9. 関連

- シナリオ正本: scenario-data.js PRD-SCN-420T（status更新済み 2026-07-17）
- 先行: README_prod423_bizcard_lang_worklog_20260716.md（ja行・多言語OFF）／README_prod420_worklog_20260715.md（判定ルール仮説）
- プラン: 多言語判定ロジック真理値表テストプラン_20260716.md
- 仕様正本: Backlog Wiki 25_多言語対応方針／61_対応言語とフォールバック／63_多言語業務フロー
